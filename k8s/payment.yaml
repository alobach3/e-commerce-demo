apiVersion: v1
kind: Service
metadata:
  name: payment-service
  labels:
    app: payment
spec:
  type: ClusterIP
  selector:
    app: payment
  ports:
    - name: grcp #grcp - http
      port: 50052
      targetPort: 50052

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment
  labels:
    app: payment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
  template:
    metadata:
      labels:
        app: payment
    spec:
      #serviceAccountName: demo-service-account ; it is using default service account;
      containers:
        - name: payment
          image: alobach3/payment-git:6634b37d2a4678d19c063533823857f6092f2fd5
          ports:
            - containerPort: 50052